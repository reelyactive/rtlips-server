#!/usr/bin/env node

var path = require('path');
var fs = require('fs');
var lib = path.join(path.dirname(fs.realpathSync(__filename)), '../lib');

var rtlips = require(lib + '/server.js');
var options = {};

var app = new rtlips(options);

